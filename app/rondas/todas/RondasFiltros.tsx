"use client";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";
import { FaFilter, FaTimes } from "react-icons/fa";
export default function RondasFiltros({ initial, reportadores }: { initial: Record<string, string>; reportadores: { id: string; nombre: string }[] }) { const router = useRouter(); const [f, setF] = useState(initial); const submit = (e: FormEvent) => { e.preventDefault(); const p = new URLSearchParams(); Object.entries(f).forEach(([k, v]) => { if (v) p.set(k, v); }); router.push(`/rondas/todas${p.size ? `?${p}` : ''}`); }; return <form onSubmit={submit} className="mb-5 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-3 flex items-center gap-2 font-extrabold text-slate-800"><FaFilter className="text-red-700" />Filtros</div><div className="grid grid-cols-1 gap-3 md:grid-cols-2 xl:grid-cols-5"><label className="text-sm font-bold text-slate-700">Médico<select className="input mt-1" value={f.usuarioId} onChange={(e) => setF({ ...f, usuarioId: e.target.value })}><option value="">Todos</option>{reportadores.map((u) => <option key={u.id} value={u.id}>{u.nombre}</option>)}</select></label><label className="text-sm font-bold text-slate-700">EPS<input className="input mt-1" placeholder="Buscar EPS" value={f.eps} onChange={(e) => setF({ ...f, eps: e.target.value })} /></label><label className="text-sm font-bold text-slate-700">Desde<input type="date" className="input mt-1" value={f.desde} onChange={(e) => setF({ ...f, desde: e.target.value })} /></label><label className="text-sm font-bold text-slate-700">Hasta<input type="date" className="input mt-1" value={f.hasta} onChange={(e) => setF({ ...f, hasta: e.target.value })} /></label><label className="text-sm font-bold text-slate-700">Paciente / documento<input className="input mt-1" placeholder="Nombre o identificación" value={f.q} onChange={(e) => setF({ ...f, q: e.target.value })} /></label></div><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={() => router.push('/rondas/todas')} className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2 font-semibold hover:bg-slate-50"><FaTimes />Limpiar</button><button className="rounded-lg bg-red-700 px-4 py-2 font-bold text-white hover:bg-red-800">Aplicar filtros</button></div></form>; }
